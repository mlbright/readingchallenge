<div class="password-setup">
  <h2>Set Up Your Password</h2>
  
  <p>Welcome, <strong><%= @user.username %></strong>! Before you can use the Reading Challenge application, please create a strong password.</p>
  
  <div class="password-requirements">
    <h3>Password Requirements:</h3>
    <ul>
      <li>Minimum 12 characters</li>
      <li>At least one lowercase letter (a-z)</li>
      <li>At least one uppercase letter (A-Z)</li>
      <li>At least one digit (0-9)</li>
      <li>At least one special character (!@#$%^&*()_+-=[]{}|;:,.<>?)</li>
    </ul>
  </div>
  
  <%= form_with url: setup_password_path, method: :post, local: true do |f| %>
    <div class="form-group">
      <%= label_tag :password, "New Password" %>
      <%= password_field_tag :password, nil, required: true, class: "form-control", minlength: 12 %>
    </div>

    <div class="form-group">
      <%= label_tag :password_confirmation, "Confirm Password" %>
      <%= password_field_tag :password_confirmation, nil, required: true, class: "form-control", minlength: 12 %>
    </div>

    <%= submit_tag "Set Password", class: "btn btn-primary" %>
  <% end %>
</div>
